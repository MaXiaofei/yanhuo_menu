# 生产/容器部署：连 compose 内的 db 容器（service name）
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://yanhuo-mysql:3306/yanhuo?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: MYSQL_PWD_REDACTED
    type: com.zaxxer.hikari.HikariDataSource
  data:
    redis:
      host: yanhuo-redis
      port: 6379

yanhuo:
  upload-dir: /app/uploads
